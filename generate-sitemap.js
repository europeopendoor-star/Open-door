import fs from 'fs';
import { blogPosts } from './src/data/blogPosts.js'; // Need to make this workable or use a commonjs approach

// Since we use ts, it's easier to write a node script that reads the files or reads TS directly.
// Actually, let's just write a script that compiles the TS first or uses a pure JS version.
